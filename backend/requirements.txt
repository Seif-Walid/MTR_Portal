fastapi>=0.115,<1
uvicorn[standard]>=0.30
SQLAlchemy>=2.0,<3
alembic>=1.13
pydantic>=2.7,<3
pydantic-settings>=2.2
psycopg[binary]>=3.1
bcrypt>=4.1
python-multipart>=0.0.9
httpx>=0.27

# Google Sheets inventory mirror (optional feature; safe to omit — the app
# degrades gracefully and disables the Sync button when unavailable)
gspread>=6.0
google-auth>=2.0

# Local .xlsx/.csv import (Inventory -> Import from file, no Google Sheets needed)
openpyxl>=3.1

